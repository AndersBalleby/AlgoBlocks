export default function UnsupportedScreen() {
  return (
    <div id="unsupported-screen">
      <div id="unsupported-box">
        <span id="unsupported-icon">💻</span>
        <h2>Skærm ikke understøttet</h2>
        <p>
          Dette værktøj er designet til brug på en computer. Prøv at åbne siden
          på en laptop eller med en større skærm.
        </p>
      </div>
    </div>
  );
}
